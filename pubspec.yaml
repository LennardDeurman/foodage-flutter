name: foodage
description: Finding a new way towards food inspiration

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  camera: ^0.8.1
  google_fonts: ^2.0.0
  provider: ^5.0.0
  rxdart: ^0.26.0
  flutter_bloc: ^7.0.0
  photo_gallery: ^1.0.1
  permission_handler: ^7.0.0
  get_it: ^6.1.1
  transparent_image: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

